schema = {
    'type': {
        'type': 'string',
        'required': True,
        'unique': True,
        'max': 100
    }
}

terrainTypes = {
    'item_url': 'int',
    'id_field': 'id',
    'schema': schema
}
